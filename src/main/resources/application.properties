spring.datasource.url=jdbc:mysql://localhost:3306/projectdb
spring.datasource.username=root
spring.datasource.password=kohli@18

server.port=8081

spring.jpa.show-sql=true

spring.mvc.view.prefix=/WEB-INF/jsps/
spring.mvc.view.suffix=.jsp


server.servlet.context-path=/locationweb


spring.mail.host=smtp.gmail.com 
#Host:: To which we want to connect,to send the email. Here we are using Gmail host           
spring.mail.port=587
#Gmail allows us to connect on 587. This is the port they have exposed out to use smtp protocol
spring.mail.username=endtoendspringbootproject@gmail.com
spring.mail.password=rohit@45
spring.mail.properties.mail.smtp.starttls.enable=true
#tls stands TransportLayerSecurity. Many email hosts require a secured transport layer to establish,thats why we setting this property as true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.auth=true
#Authentication is required

#i)By default google will not allow external applications to connect /send emails to our hosted server,
                            #we need to disable that setting by going to this url:www.google.com/settings/security/lesssecureapps   then Turn ON::Allow less secure apps: ON
#ii)ON::: This is required in order,for our app to use the smtp host throgh our login and send emails.   
